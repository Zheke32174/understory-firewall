<?xml version="1.0" encoding="utf-8"?>
<!--
  :overlay-i2p library manifest. Merged into the consumer app (browser
  today; possibly firewall in phase γ) at build time.

  The service is exported="false" — it can only be started from within
  the same UID. The companion-app pattern in the suite is "browser
  starts the supervisor; firewall observes I2pStatus to render
  status, never reaches across UIDs to start it." Same-UID is the
  enforcement mechanism.

  Foreground service type "specialUse" is the closest official fit for
  a userspace network proxy — same choice the firewall's VpnService
  makes. The PROPERTY_SPECIAL_USE_FGS_SUBTYPE makes the use case
  inspectable by audit tools.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required by every consumer of the supervisor. Listed here so
         the manifest merger lands them on each app that depends on us. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>

        <service
            android:name=".I2pProxyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="userspace_network_proxy" />
        </service>

    </application>

</manifest>
