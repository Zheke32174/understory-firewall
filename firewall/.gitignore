# dnscrypt-proxy binaries are generated by tools/fetch-dnscrypt-proxy.sh
# from the upstream release tarballs. Verifying SHA-256 happens inside
# that script. Don't commit the binaries themselves; the script is the
# source of truth.
src/main/jniLibs/*/libdnscrypt-proxy.so
